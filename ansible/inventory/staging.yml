---
all:
  children:
    staging:
      hosts:
        triggertime-staging:
          ansible_host: "ssh.triggertime.es"
          ansible_port: 2222
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
          ansible_python_interpreter: /usr/bin/python3
      vars:
        env_name: staging
