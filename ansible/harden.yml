---
- name: Harden server
  hosts: all
  become: true
  vars_files:
    - "vault/{{ env_name }}.yml"
  roles:
    - ssh
    - firewall
    - unattended-upgrades
    - certbot
    - nginx
    - postgres
    - backups
    - monitoring
    - cloudflare
